1.2~ 
텍스트 데이터 분석이란 -0
다양한 형태의 텍스트 데이터로부터 
고품질의 정보를 도출하기 위해 
협업의 텍스트 데이터를 변환 및 정제하여
추출된 단어 관계 및 패턴 , 규칙을 분석하는 능력

3. t-test 관련된문제  -0
4. t-test 관련된문제   

두개의 집단 간 차이가 의미가 있는지 보는것 - 0
T-test, ANOVA(3개 집단), ANcova, manova 
명목변수 -> 연속변수

유형 - 0
독립표본 -> 그룹간의 평균 비교 (ex 남자와 여자 간 소득의 차이비교)
대응표본 -> 하나의 집단에 대한 비교 (ex 과외를 하기 전과 후의 반 학생들의 성적변화)
단일표본 -> 특정 집단의 평균이 어떤 숫자와 같은지 다른지를 비교

조건
등분산성, 정규 분포여부, 결과값이 연속변수인지 아닌지 -0

결과변수(종속변수)  - 0
원인변수(독립변수)

정규 분포 테스트(Shapiro Test) - 0
 귀무 가설 : 정규분포가 맞다
 대립 가설 : 정규분포가 아니다

등분산 테스트 -0
 귀무가설: 등분산이 맞다(남성과 여성의 평균나이의 차이가 없다)
 대립가설: 등분산이 아니다(남성과 여성의 평균나이의 차이가 있다)


5. 상관분석(서술형) - 0
 1:1로 서로 동등한 입장에서 관계를 분석
- 두 변수를 연속형 변수이어야 한다.
- 선형 함수이어야 한다. 
- 상관계수(r계수) : 1 ~ -1, 설명 계수(r ^2) : 0.5 ^2 = 0.25 
- 피어슨의 상관 계수, 스피어만 상관계수, kendall의 tau (피어슨은 연속 변수->모수적인 방식 , 스피어 and kendall은 서열 변수-> 비모수적인 방식)

6. 회귀분석 - 0
인과 관계
- 1(독립변수, 원인변수):1(종속변수,결과변수)  -> 단일 회귀분석
- 다:1 -> 다중회귀분석

- 조건
- 연속변수(선형성)
- 정규분포
- 등분산
- 다중 공선성 작아야 한다.(독립성)
- 일반 선형 모델 (Ordinary Linear Model) - 최소 제곱법
- 일반화 선형 모델 (General Linear Model) - 로지스틱 회귀 분석, 포아송 회귀분석


7. 정규표현식
8. 정규표현식
 - 반복
        * -> 0번 이상 
        + -> 1회 이상
        ? -> 0회 또는 1회 
        {m} -> m회 반복
        {m,n}-> m회 부터 n회까지 반복 
        
        
    - 매칭 메타 문자
        . -> 줄바꿈 문자를 제외한 모든 문자와 매치
        ^ -> 문자열의 시작과 매치
        $ -> 문자열의 마지막과 매치
        [ ] -> 문자집합 중 한 문자를 의미
        | -> 또는(or)의 의미
        { } ->  정규식을 그룹을 묶음 
        
        
        
    - 특수 문자(\문자)
        \d -> 모든 숫자와 매치
        \D -> 숫자가 아닌 문자와 매치
        \s -> 공백문자와 매치
        \S -> 공백문자가 아닌 모든 것과 매치
        \w -> 숫자 또는 문자와 매치
        \W -> 숫자 또는 문자가 아닌 모든 문자와 매치 
        
        
    - python에서 제공하는 메서드
        compile() -> 정규표현식 객체를 생성 (패턴 정의)
        match() -> 문자열의 처음부터 정규식과 매치되는지 조사
        search() -> 문자열 전체를 검색하여 정규식과 매치되는지 조사
        split() -> 문자열 분리
        findall() -> 정규식과 매치되는 모든 문자열을 리스트로 리턴
        finditer() -> 정규식과 매치되는 모든 문자열을 반복 가능한 객체로 리턴 
        sub() -> 데이터를 변경하거나 교체


9. text mining 기반 데이터분석 : p16 - 0
파싱 -> 입력 텍스트를 의미있는 토큰으로 분해하고 문장 구조를 생성하는 과정
단어 -> 어절은 하나 혹은 두개이상의 단어로 구성
형태소는 의미가 있는 최소의 단위
어간을 분리해 내기 위해 사용되는 텍스트 처리기술 -> 스테밍
스테밍 -> 어형이 변형된 단어로부터 어간을 분리 

10. text mining 기반 데이터분석 : p17 - 0
텍스트 전처리 -> 문장 분리, 불필요한 문장 성분 제거

(1) 입력 텍스트를 문장부호(마침표, 쉼표)로 분리
(2) 문장부호, 특수문자, 숫자 등 불필요한 성분 제거
(3) 어절을 분리(띄어쓰기 기준)


###################################33333
실습형 
1.  단일 t-test 문제 
2.  독립 t-test 문제
3.  anova 문제
4.  카이제곱분석 문제
5.  선형회귀 분석 문제 